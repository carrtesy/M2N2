program: test.py
method: grid
name: Pump_MLP
project: OnlineTSAD
entity: carrtesy
metric:
  name: F1
  goal: maximize
parameters:
  dataset:
    value: "Pump"
  thlr:
    values: [0.01, 0.005, 0.0001, 0.0005, 0.00001, 0.00005, 0.000001]
  exp_id:
    value: default


command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
